export const djinnjsOutDir = 'REPLACE_WITH_OUTPUT_DIR_NAME';
export const gaId = document.documentElement.dataset.gaId;
export const isDebug = document.documentElement.getAttribute('debug') ? true : false;
export const environment = document.documentElement.dataset.environment;
